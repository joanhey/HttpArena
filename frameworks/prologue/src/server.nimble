# Package
version       = "0.1.0"
author        = "HttpArena"
description   = "HttpArena Prologue benchmark"
license       = "MIT"
bin           = @["server"]

# Dependencies
requires "nim >= 2.0.0"
requires "prologue >= 0.6.0"
requires "zippy >= 0.10.0"
